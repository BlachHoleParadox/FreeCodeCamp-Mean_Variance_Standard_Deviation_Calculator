# Start by reading the README.md file. This file is intended to be used as an entry point during development.
import mean_var_std
from unittest import main

print(mean_var_std.calculate([0,1,2,3,4,5,6,7,8]))

# Unit Test
main(module='test_module', exit=False)
